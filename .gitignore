# Runtime state
.env
keys/*
!keys/.gitkeep
workspace/**
# Python/venvs
**/.venv/**
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
# Node
**/node_modules/**
# Docker artifacts
*.tar

# Python / build
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Local runtime
.claude/
.swp
.DS_Store

# Sandbox mirrors
sandbox-home/
sandbox-shared_workspace/

# Local Makefile backups
Makefile.bak.*

# Local Makefile backups
Makefile.bak.*
# SSH keys: keep only public keys in git
keys/*
!keys/*.pub
# MacEff customization layer (user-specific, created by maceff-init)
# NOTE: gitignore treats '#' as a comment only at the START of a line. These
# four patterns carried trailing inline comments, which made each pattern the
# literal string "path/ # comment ..." — matching nothing. They looked
# configured and ignored nothing for as long as they had existed.
#
# These use the "dir/*" form rather than "dir/", because a re-include cannot
# resurrect a file whose PARENT DIRECTORY is excluded — git never descends into
# an excluded directory, so "!dir/.gitkeep" under "dir/" silently does nothing.
# The scaffolding below has to survive: a .gitkeep whose whole purpose is to be
# tracked, and the starter config a fresh clone needs.

# All generated framework content (policies, templates, commands, skills, ...)
.maceff/framework/*
!.maceff/framework/.gitkeep

# User configuration, minus the shipped starters
.maceff/config/*
!.maceff/config/.gitkeep
!.maceff/config/config.json.example
!.maceff/config/global.env
!.maceff/config/projects/
.maceff/config/projects/*
!.maceff/config/projects/demo.env

# Session state files (runtime artifacts)
.maceff/sessions/
# Agent cycle/state tracking (runtime artifact)
.maceff/agent_state.json
# Agent event log (runtime artifact, append-only and unbounded)
.maceff/agent_events_log.jsonl

# Local backup files
docker-compose.yml.bak.*
docker/start.sh.bak.*
tools/bin/*.bak.*
LEGACY_POLICIES_NO_VCS/
.git.backup-before-redaction/

# Local docker-compose overrides (environment-specific)
docker-compose.override.yml

# Voice training data and domain-specific correction patterns (local data, not code)
macf/src/macf/voice/training_data/
macf/src/macf/voice/fusion_patterns.json

