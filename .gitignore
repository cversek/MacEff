# Runtime state
.env
keys/*
!keys/.gitkeep
workspace/**
# Python/venvs
**/.venv/**
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
# Node
**/node_modules/**
# Docker artifacts
*.tar

# Python / build
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Local runtime
.claude/
.swp
.DS_Store

# Sandbox mirrors
sandbox-home/
sandbox-shared_workspace/

# Local Makefile backups
Makefile.bak.*

# Local Makefile backups
Makefile.bak.*
# SSH keys: keep only public keys in git
keys/*
!keys/*.pub
# MacEff customization layer (user-specific, created by maceff-init)
.maceff/framework/         # All framework content (policies, templates, agent_defs)
.maceff/config/            # All configuration (env files, config.json)
.maceff/sessions/          # Session state files (runtime artifacts)
.maceff/agent_state.json   # Agent cycle/state tracking (runtime artifact)

# Optional: Keep example templates in git if needed
# !.maceff/config/config.json.example
# !.maceff/framework/.gitkeep

# Local backup files
docker-compose.yml.bak.*
docker/start.sh.bak.*
tools/bin/*.bak.*
LEGACY_POLICIES_NO_VCS/
.git.backup-before-redaction/

# Local docker-compose overrides (environment-specific)
docker-compose.override.yml
