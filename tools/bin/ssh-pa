#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=common.sh
. "${SCRIPT_DIR}/common.sh"

key="$(find_pa_key)"
exec ssh -i "${key}" -p "${PORT}" "${PA}@localhost"
