# Admin + Jupyter user inside the container
ADMIN_USER=maceff
JUPYTER_USER=maceff

# Extra users (comma-separated). Optional UID after colon.
#USERS=alice,bob:1002
SUDO_USERS=maceff

# Optional fixed Jupyter token (otherwise generated each start)
# JUPYTER_TOKEN=please-change-me

# Claude API key (recommended: export in your shell instead of here)
# ANTHROPIC_API_KEY=sk-ant-...
