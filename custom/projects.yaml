# MacEff v0.3.0 Project Configuration - Test Setup
# Phase 2 smoke test configuration

projects:
  TestProject:
    # Project context file (Layer 3 - Project)
    context: ../custom/projects/TestProject_context.md

    # No repos for smoke test (can add later)
    repos: []

    # No data mounts for smoke test
    data_mounts: []

    # No custom commands for smoke test
    commands: {}
